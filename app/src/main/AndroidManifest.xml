<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shiyan1">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shiyan1">
        <activity
            android:name=".view.activities.AddHaoyouActivity"
            android:exported="true" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FriendsAppMainActivity"
            android:exported="true"></activity>
        <activity
            android:name=".MajorSettingActivity"
            android:exported="true" />
        <activity
            android:name=".ZitidaxiaoActivity"
            android:exported="true" />
        <activity
            android:name=".TongxueHaoyouActivity"
            android:exported="true" />

        <provider
            android:name=".MyContentProvider"
            android:authorities="com.example.shiyan1"
            android:enabled="true"
            android:exported="true" /> <!-- <provider -->
        <!-- android:name=".HaoyouProvider" -->
        <!-- android:authorities="com.example.shiyan1" -->
        <!-- android:enabled="true" -->
        <!-- android:exported="true" /> -->
        <activity
            android:name=".BianjiActivity"
            android:exported="true" />
        <activity
            android:name=".XiangxixinxiActivity"
            android:exported="true" /> <!-- <activity -->
        <!-- android:name=".HaoyouliebiaoActivity" -->
        <!-- android:exported="true" -->
        <!-- android:label="好友列表"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <service
            android:name=".MyService"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>