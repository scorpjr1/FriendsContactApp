<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10sp"
    app:chainUseRtl="true"
    android:orientation="vertical">

    <TextView
        android:id="@+id/tv_dcis_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Image Action"
        android:textColor="#333"
        android:textSize="16dp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintBottom_toTopOf="@id/tv_dcis_camera" />

    <TextView
        android:id="@+id/tv_dcis_camera"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="30dp"
        android:gravity="center"
        android:text="Camera"
        android:textColor="#333"
        android:textSize="15dp"
        android:drawableTop="@drawable/ic_baseline_camera"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintHorizontal_chainStyle="spread"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/tv_dcis_gallery"
        app:layout_constraintTop_toBottomOf="@id/tv_dcis_title" />

    <TextView
        android:id="@+id/tv_dcis_gallery"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="30dp"
        android:gravity="center"
        android:text="Gallery"
        android:textColor="#333"
        android:textSize="15dp"
        android:drawableTop="@drawable/ic_baseline_image"
        app:layout_constraintHorizontal_chainStyle="spread"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toRightOf="@id/tv_dcis_camera"
        app:layout_constraintTop_toBottomOf="@id/tv_dcis_title" />
<!--    app:drawableTopCompat="@drawable/ic_baseline_image"-->
</androidx.constraintlayout.widget.ConstraintLayout>